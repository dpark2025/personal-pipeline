# NPM Registry Configuration for Personal Pipeline
# This configures the project to use the private NPM registry

# Private registry for Personal Pipeline packages
registry=http://localhost:4873/

# Scoped registry for @personal-pipeline packages
@personal-pipeline:registry=http://localhost:4873/

# Authentication token placeholder (will be set after login)
//localhost:4873/:_authToken=

# Always authenticate with private registry
always-auth=true

# Package access configuration
access=restricted

# Disable package-lock for private packages (optional)
# package-lock=false

# Registry specific settings
save-exact=true
engine-strict=true